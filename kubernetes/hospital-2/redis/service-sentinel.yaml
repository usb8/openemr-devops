apiVersion: v1
kind: Service
metadata:
  name: hospital-2-sentinel
spec:
  clusterIP: None
  ports:
  - port: 5000
    targetPort: 5000
    name: hospital-2-sentinel
  selector:
    app: hospital-2-sentinel